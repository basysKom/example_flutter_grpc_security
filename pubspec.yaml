name: example_flutter_grpc_security
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
    
  grpc: ^4.0.1
  dart_jsonwebtoken: ^2.14.1

  grpc_interface:
    path: packages/grpc_interface

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/cert.pem